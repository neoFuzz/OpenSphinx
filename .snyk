# Snyk policy file — https://docs.snyk.io/snyk-cli/commands/ignore
version: v1.25.0

exclude:
  global:
    # Never scan installed dependencies — only scan source code.
    - node_modules/**
    # Legacy Node.js server archived after Cloudflare Workers migration.
    - _archive/**